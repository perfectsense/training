package brightspot.core.requestextras;

/**
 * HTTP Method Names for the purpose of {@link PathRequestMatcher}.
 */
public enum HttpMethodName {
    HEAD,
    GET,
    POST,
    OPTIONS,
    PUT,
    DELETE,
    PATCH
}
