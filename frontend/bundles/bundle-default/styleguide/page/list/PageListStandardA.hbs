{{#if items}}
<div class="PageListStandardA" data-module {{include "includes/PageList-attributes.hbs" this}}>
    {{include "/global/ModuleHeader.hbs" this}}

    <div class="PageList-items">
        <div class="PageList-items-item">
            {{#with items.[0]}}
                {{#eq this._template "/page/promo/PagePromo.hbs"}}
                    {{#set imageSize="large-16x9"}}
                        {{#set sizes="(max-width: 1024px) 100vw, 1024px"}}
                            {{include "/page/promo/PagePromo.hbs" this}}
                        {{/set}}
                    {{/set}}
                {{else}}
                    {{render this}}
                {{/eq}}
            {{/with}}
        </div>
        {{#each items}}
            {{#if (gt @index 0)}}
                {{#if (lt @index 5)}}
                    <div class="PageList-items-item">
                        {{#eq this._template "/page/promo/PagePromo.hbs"}}
                            {{include "/page/promo/PagePromo.hbs" this media=null authorName=null description=null}}
                        {{else}}
                            {{render this}}
                        {{/eq}}
                    </div>
                {{/if}}
            {{/if}}
        {{/each}}
        {{#each items}}
            {{#if (gt @index 4)}}
                {{#if (lt @index 9)}}
                    <div class="PageList-items-item">
                        {{#eq _template "/page/promo/PagePromo.hbs"}}
                            {{#set imageSize="medium-4x3"}}
                                {{#set sizes="(max-width: 568px) 100px, 500px"}}
                                    {{include "/page/promo/PagePromo.hbs" this description=null mobileAltLayout="true"}}
                                {{/set}}
                            {{/set}}
                        {{else}}
                            {{render this}}
                        {{/eq}}
                    </div>
                {{/if}}
            {{/if}}
        {{/each}}
    </div>

    {{include "includes/PageList-footer.hbs" this}}
</div>
{{/if}}
