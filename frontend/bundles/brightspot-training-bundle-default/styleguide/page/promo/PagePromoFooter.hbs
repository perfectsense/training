<div class="PagePromoFooter"{{include "includes/PagePromoAttributes.hbs" this}}>
    <div class="PagePromoFooter-content">
        {{include "includes/PagePromoTitle.hbs" this}}
        {{include "includes/PagePromoDescription.hbs" this}}

        {{#if ctaButtonText}}
            {{#if url}}
                <a class="Button" href="{{url}}"{{#with target}} target="{{this}}"{{/with}}>{{ctaButtonText}}</a>
            {{/if}}
        {{/if}}
    </div>
</div>
