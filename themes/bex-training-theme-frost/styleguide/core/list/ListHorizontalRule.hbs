<div class="ListHorizontalRule"{{extraAttributes}}
    {{#if backgroundColor}}style="--color:var(--{{backgroundColor}})"{{/if}}
    {{#if inverseColors}}data-inverse-colors {{/if}}
    {{#with horizontalAlignment}} data-horizontal-align="{{this}}"{{/with}}>
    <div class="ListHorizontalRule-heading">
        {{#with media}}
            <div class="ListHorizontalRule-media">
                {{this}}
            </div>
        {{/with}}
        {{#with title}}
            <div class="ListHorizontalRule-title">
                {{this}}
                <hr class="ListHorizontalRule-title-hr">
            </div>
        {{/with}}
        {{#with description}}
            <div class="ListHorizontalRule-description">{{this}}</div>
        {{/with}}
    </div>
    {{#with items}}
        <ul class="ListHorizontalRule-items">
            {{#each this}}
                <hr class="ListHorizontalRule-items-hr">
                <li class="ListHorizontalRule-items-item">
                    {{render this [/core/promo/Promo.hbs]="/core/promo/PromoTextItem.hbs"}}
                </li>
            {{/each}}
        </ul>
    {{/with}}
    {{#if ctaUrl}}
        <div class="ListHorizontalRule-cta">
            {{#if ctaText}}
                {{include "/core/link/SolidButton.hbs" href=ctaUrl target=ctaTarget body=ctaText}}
            {{else}}
                {{include "/core/link/SolidButton.hbs" href=ctaUrl target=ctaTarget body=(format "/core/list/List" "ctaText")}}
            {{/if}}
        </div>
    {{/if}}
</div>
