import Input from './Input.js'

export default class EmailInput extends Input {
  // If input is required, validation is required,
  // This is the implementation in Input, so no override
  // is necessary.
}
