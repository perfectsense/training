{{include "/core/search/SearchResultsModule.hbs"
          sorts=sorts
          filters=filters
          items=items
          count=count
          previousPageUrl=previousPageUrl
          nextPageUrl=nextPageUrl
          pageCount=pageCount
          pageNumber=pageNumber
          fe=this
}}
