{{#if ampEnabled}}
    <amp-ad width="{{ampWidth}}"
        height="{{ampHeight}}"
        type="doubleclick"
        data-slot="{{slot}}"
        data-multi-size="{{ampSizes}}"
        data-multi-size-validation="{{ampMultiSizeValidation}}">
    </amp-ad>
{{/if}}
