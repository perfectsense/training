{{#if items}}
    <div class="AssetRequestTable">
        <table>
            {{include "/dam/table/PaginatedTable-head.hbs" this}}
            {{include "/dam/table/PaginatedTable-items.hbs" this}}
        </table>
    </div>
    <div class="AssetRequestTable-pagination">
        {{#if pageCount}}
            <span class="AssetRequestTable-pagination-pageCount">{{pageNumber}} of {{pageCount}}</span>
        {{/if}}
        <a {{#if previousPage}}href="{{previousPage}}"{{else}}data-disabled{{/if}} rel="prev" class="AssetRequestTable-pagination-previous" title="Previous"></a>
        <a {{#if nextPage}}href="{{nextPage}}"{{else}}data-disabled{{/if}} rel="next" class="AssetRequestTable-pagination-next" title="Next"></a>
    </div>
{{else}}
    <h3 class="AssetRequestTable-noResults">
        There are no items to show.
    </h3>
{{/if}}
